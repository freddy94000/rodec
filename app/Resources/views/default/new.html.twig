{% extends 'base.html.twig' %}

{% block title %}
    {{ news.title }} - {{ parent() }}
{% endblock %}

{% block body %}
    <section id="content">
        <div class="container_24">
            <div class="wrapper">
                <article class="grid_24">
                    <h1>{{ news.title }}</h1>
                    {{ news.content|raw }}
                </article>
            </div>
        </div>
    </section>
{% endblock %}
